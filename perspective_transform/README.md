# perspective_transform

## Synopsis

`perspective_transform` contains the perspective transform that was written for the 2022 competition. This perspective transform listens to the `/zed/zed_node/left/image_rect_gray` and publishes the bird's eye view to `/cv/perspective/left`.